Sources
        - EBI ftp share site
                http://ftp.ebi.ac.uk/pub/contrib/glygen/
        - Download coordinate data
                curl -X GET --header 'Accept:application/json' \
                'https://www.ebi.ac.uk/proteins/api/coordinates?offset=0&size=-1&taxid=9606' \
                > UP000005640_9606.coordinates.json

                curl -X GET --header 'Accept:application/json' \
                'https://www.ebi.ac.uk/proteins/api/coordinates?offset=0&size=-1&taxid=10090' \
                > UP000000589_10090.coordinates.json


1) Data location
        >datadir=/data/projects/glygen/downloads/uniprot/rdf/

2) List bad values (this will create $originalRdfFile-badvalues)
        >list-badvalues.py -i $originalRdfFile -d $outDir

3) Editing bad values (this will use $originalRdfFile-badvalues created in step2, and will create
   a new edited version of $originalRdfFile)
        >python edit-rdf.py -i $originalRdfFile -d #outDir

4) Coverting edited RDF file to nt
        >nohup python rdf2ntriples.py -i $editedRdfFile &

5) Partition resulted nt file
        python partition-nt.py -i $ntFile

6) Make sure virituoso is running, if not follow these instructions
        > cd /usr/local/var/lib/virtuoso/db/
        > sudo rm -f /usr/local/var/lib/virtuoso/db/virtuoso.trx
        > sudo virtuoso-t
        > /usr/local/bin/isql localhost:1111 dba dba
        from >SQL console, type "tables;" to see tables

7) Edit virtuoso-load.sh and run it as
        > nohup sh virtuoso-load.sh &

        Connect and use these sql to see load status of each n3 partition
        > /usr/local/bin/isql localhost:1111 dba dba
        SQL> select ll_file, ll_state from DB.DBA.load_list;


=====================
SPARQL Tutorial
        >https://www.w3.org/2009/Talks/0615-qbe/


